export const QR_LIFETIME_PATH = "/qr-lifetime";

export function qrUnavailablePath(reason: "archived" | "missing"): string {
  return `/unavailable?reason=${reason}`;
}

/** Public policy for printed QR after trial, unpaid period, archive, or delete. */
export const QR_LIFETIME = {
  updated: "2026-09-18",
  title: "Как долго работает напечатанный QR-код",
  downloadDynamic:
    "В файл попадает короткая ссылка. После печати можно сменить адрес без нового тиража, пока код не удалён из библиотеки. Окончание тарифа или пробного периода не отключает уже напечатанный динамический QR; новые динамические коды и смена назначения снова понадобятся после оплаты.",
  downloadStatic:
    "Статический код навсегда содержит текущую ссылку внутри картинки. Сервер не сможет её сменить или отключить после печати.",
  billing:
    "Короткие ссылки уже скачанных динамических кодов продолжают открываться после окончания тарифа или пробного периода. Новые динамические QR, смена назначения и платные лимиты станут недоступны, пока тариф снова не оплачен. Удаление кода из библиотеки отключает короткую ссылку.",
  deleteDynamic:
    "Напечатанный динамический QR перестанет открываться: короткая ссылка будет отключена. Это нельзя отменить из кабинета. Статический код в картинке так не отключается.",
  deleteStatic:
    "QR будет скрыт из библиотеки. Если код уже напечатан, картинка со статическим содержимым продолжит работать — сервер её не отключает.",
  archivedTitle: "QR-код отключён",
  archivedDescription:
    "Владелец удалил этот QR-код из библиотеки, поэтому короткая ссылка больше не открывается.",
  missingTitle: "QR-код не найден",
  missingDescription: "Такой короткой ссылки нет. Проверьте, что код считался целиком.",
} as const;

export function qrDeleteConfirm(kind: "STATIC" | "DYNAMIC"): string {
  return kind === "DYNAMIC" ? QR_LIFETIME.deleteDynamic : QR_LIFETIME.deleteStatic;
}
